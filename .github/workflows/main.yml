name: CI-CD
on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Obtendo o codigo
        uses: actions/checkout@v4.2.2
      
      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.SECRET_RACO1 }}
          password: ${{ secrets.SECRET_RACO }}
      
      - name: Construcao e envio da Imagem Docker
        uses: docker/build-push-action@v6
        with:
          context: ./src
          push: true
          file: ./src/Dockerfile
          tags: raco21/fake-shop-desafio:v${{ github.run_number }}

  CD:
    needs: [CI]
    runs-on: ubuntu-latest
    steps:       
      - run: echo "obeter codigo"
      - run: echo "Executar o kubeconfig"
      - run: echo "Executar apply"
